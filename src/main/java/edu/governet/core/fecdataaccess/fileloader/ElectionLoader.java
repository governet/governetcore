package edu.governet.core.fecdataaccess.fileloader;

import edu.governet.core.api.AppContext;

public class ElectionLoader {
    AppContext context;

    public ElectionLoader(AppContext context) {
        this.context = context;
    }
}
